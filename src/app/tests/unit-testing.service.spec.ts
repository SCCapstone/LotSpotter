import { TestBed } from '@angular/core/testing';

describe('UnitTests', () => {

  /* beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(UnitTestingService);
  }); */

  it('lot data should be valid', () => {
    expect(true).toBeTruthy();
  });
});
